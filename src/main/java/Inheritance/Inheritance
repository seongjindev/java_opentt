상속
어떤 개체가 있을 때 그 객체의 필드(변수)와 메소드를 다른 개체가 물려 받을 수 있는 기능을 상속이라고 한다.

부모 클래스와 자식 클래스의 관계를 상위(super) 클래스와 하위(sub) 클래스라고 표현하도 한다.
또한 기초 클래스(base class), 유도 클래스(derived class)라고도 부른다.

extends를 사용

클래스 다이어그램 - 상위 클래스로 향하는 방향표시

코드중복제거, 재활용성 증가